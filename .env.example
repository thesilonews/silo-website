# Neon Postgres — both values come from Vercel dashboard after connecting Neon storage
DATABASE_URL="postgresql://..."   # pooled connection (used by Prisma at runtime)
DIRECT_URL="postgresql://..."     # direct connection (used by Prisma migrations/push)

# Buttondown
BUTTONDOWN_API_KEY=
